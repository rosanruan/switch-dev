# Changelog

本文件记录 Switch Dev 每个版本的变更。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

发布时，`make release` 会自动从本文件提取对应版本的章节作为 GitHub Release Notes。

更新分级规则：
- **patch（0.0.x）**：普通更新，用户可忽略/稍后更新
- **minor（0.x.0）/ major**：强制更新，客户端弹窗不可忽略

维护方式：
- 日常开发：改动随手往下面 `[Unreleased]` 章节加一条（写给用户看的话，不是 commit 信息）
- 忘记记录了：`make changelog-auto` 从上个 tag 到 HEAD 的 commit 生成草稿，再人工润色
- 发布定版：`make changelog-release V=x.y.z` 自动把 `[Unreleased]` 改名为 `[x.y.z] - 日期` 并新开空 Unreleased

分类小标题固定用：`### 新增` / `### 修复` / `### 变更` / `### 移除` / `### 废弃` / `### 安全`

## [Unreleased]

### 新增
- 内置上游（JoyCode/DevEco/OpenCode）的模型清单改为实时拉取 + 本地持久化：不再写死在代码里，上游上新模型后点「🔄 刷新模型」即可用，无需等版本更新
- 拉取结果落 SQLite，重启后 `/v1/models`、Models 页和路由层立刻就是最新一份；后台每 6 小时自动刷新一次
- Models 页新增「🔄 刷新模型」按钮和各上游「实时/本地/免费」来源徽章
- 设置页的模型来源徽章移到运行模式区，手动模式和 UA 模式下也能看到（此前仅 auto 模式可见）
- 日志页分页：默认每页 50 条，可切换 20/50/100/200，带首页/上一页/下一页/末页与「共 N 条，第 X-Y 条」提示（此前一次性加载最多 500 条全部堆在一页里滚动）
- 日志页状态筛选按钮改为「本页 / 全范围」双计数（如「成功 20/45」），既能看当前页的状态分布，也保留整个时间范围的总数

### 修复
- **WorkBuddy 对话首字延迟大幅降低**：此前 WorkBuddy 未实现流式接口，流式请求会退化成「等整个回答生成完再一次性吐出」，首字要等 20-40 秒（约等于完整生成耗时）；现在改为逐块直传上游 SSE，首字延迟与官方客户端一致。其余上游（JoyCode/DevEco/OpenCode）与第三方供应商本就是真流式，不受影响
- 修复 WorkBuddy 等推理模型在**非流式**请求下思维链丢失的问题（SSE 聚合器未解析 `reasoning_content`）
- 修复 DevEco 选用「接口有、内置清单没有」的新模型时，请求被静默改写成 GLM-5.1 的问题（现在原样透传，让上游自己报错）
- 修复供应商模型列表在增删供应商与 `/v1/models` 并发读写时的数据竞争（改为原子快照发布）
- 修复 `make test` 因 Makefile 变量转义错误而实际未运行任何测试的问题

### 变更
- 降级链里遇到不支持流式的上游时，改为记录一条 `stream_unsupported` 日志再跳过（此前静默跳过，用户看不出「配的是 A、实际走了 B」）
- 上游下线的模型在数据库中只做软标记、不删行，避免历史日志与用量统计的模型名断链
- 日志页的状态筛选（成功/错误/鉴权失败/降级）改为下推到 SQL，与分页配合后页码总数按筛选后的条数计算
- JoyCode 模型现在也参与 usage 合理性校验的 context 上限钳制（此前遗漏，仅 DevEco/OpenCode/WorkBuddy 生效）
- WorkBuddy 因上游无模型列表接口，仍使用内置清单，界面标记为「本地」

## [0.1.4] - 2026-09-01

### 新增
- 检测到新版本时全局弹窗提示（不再仅限于设置页内），大版本（major/minor 变化）强制更新不可关闭，小版本（仅 patch 变化）可跳过或稍后再说
- 跳过版本持久化：小版本更新可选择「跳过此版本」，跳过的版本号写入配置，后续不再提示
- 窗口被激活时自动检查更新（长时间未操作后回到应用即触发，5 分钟防抖避免频繁请求）
- Server 模式（无 GUI）全平台裸二进制构建与发布：`make build-server-binaries` 交叉编译 Linux/macOS amd64/arm64 四个 server 产物，`make deploy` 自动上传至 GitHub Release，供服务器部署使用

### 修复
- 修复 macOS 标题栏红绿灯按钮与页面内容重叠的问题：改用 MacTitleBarHidden 替代 MacTitleBarHiddenInset，避免 NSToolbar 导致交通灯按钮侵入 header 区域
- 修复仪表盘「总请求数」始终显示 0 的问题：改从数据库 logs 表查询累计总数（重启不丢数据），并监听 log:new 事件实时递增

### 变更
- 自动更新检查间隔从 6 小时缩短为 2 小时，更快发现新版本
- Server 模式构建改用 CGO_ENABLED=0 直接交叉编译，不再依赖 Docker/QEMU 模拟，构建速度从 1 小时+降至数十秒
- `make dist` 暂不打包 Linux arm64 deb 安装包（裸二进制仍正常构建）

## [0.1.3] - 2026-08-24

### 新增
- UA 路由规则新增「规则级默认目标」（defaultTarget）：UA 命中但请求模型未命中任何具体映射时，整个客户端请求直接路由到该规则的默认上游模型，请求模型 id 原样透传；未设置时维持原行为回退 UA 全局兜底/正常降级链。这样无需为每个客户端逐一枚举其请求模型清单（cc-switch 式免维护），Claude Code 这类默认模型随版本/机器变化的客户端也能稳定分流。每条 UA 规则卡片顶部可独立选择上游+模型，可一键清除
- Windows 安装器在启动时检测旧版（≤0.1.1，管理员装到 Program Files、写 HKLM 卸载项）残留：发现则提示用户先卸载旧版再继续，避免「应用和功能」出现两个 Switch Dev 卸载项及 Program Files 残留；静默安装（/S）不弹窗。同时澄清：遇到 “Error opening file for writing ...\Program Files\...” 报错即说明误用了旧版安装包，需改用 0.1.2 之后的按用户安装包（装到 %LOCALAPPDATA%\Programs）

### 修复
- 修复 macOS 自动更新后弹出供应商「主密码」输入框、但用户从未设置过主密码（自动加密模式）导致已保存的 API Key 无法解锁的问题：新增机器绑定包裹层，用本机机器码派生密钥额外包裹 DEK，不再因自更新改变二进制签名/CDHash 被钥匙串 ACL 拒之门外；钥匙串/文件副本兜底逻辑保留。Windows（DPAPI 绑定用户/机器）本就免疫，本次一并获得同一兜底
- 自动加密模式下若所有无密码解锁路径都失效（如极端情况下钥匙串与机器码均不可用），不再显示一个不可能输对的密码框，改为「自动加密密钥不可用」专门界面，可一键**保留供应商列表、仅清空 API Key 重新配置**，无需逐个重新添加供应商；已设置主密码的用户仍走密码/恢复码流程不变
- JoyCode 灰度拒绝（AI_GRAY_ACCESS_DENIED / COLOR_FORWARD_EXCEPTION）的错误提示归类为可操作文案：明确告知「当前账号未被开通该模型的独立访问，请保持 JoyCode 官方客户端打开并登录后重试，或在管理后台开通权限」，替代原先晦涩的 raw JSON；同时确认此类拒绝会自动沿降级链尝试下一个上游（原有降级逻辑已覆盖，本次仅友好化提示）
- 修复 Windows 上自动更新下载 100% 后才报 `Access is denied`（安装目录无写权限）的问题：更新前新增安装目录可写性预检，不可写时提前失败并给出可操作指引（退出 → 卸载 Program Files 旧版 → 用最新按用户安装包重装），不再等下载完成才报错；selfupdate 的权限类错误统一归类为同样的友好指引文案，错误框支持多行显示
- 更新提示的「发现新版本」改为 `当前版本 → 新版本` 标签化展示，避免把本机当前版本误读成检测到的最新版
- 修复 Windows/macOS/Linux 可重复启动多个实例的问题：再次启动不再开第二个窗口/托盘/代理（第二个实例还会因 127.0.0.1:8787 端口占用启动失败），改为激活已有的第一个实例窗口（显示、取消最小化、聚焦）。三平台分别用命名 Mutex / flock 锁文件 / D-Bus 总线名实现
- 修复启用单实例后自动更新无法重启新版本的问题：更新后的新进程现在带 `--wait-for-pid=<旧PID>` 启动，在 `main()` 最开头等待旧进程完全退出、释放单实例锁与端口后再初始化，避免被当成「第二实例」静默自杀（15s 超时兜底）
- 修复网关鉴权失败（客户端带错 key，如把第三方供应商 Key 填进了 `ANTHROPIC_API_KEY`）时只回干巴巴的 `401 invalid api key`、且应用日志页完全看不到这些拒绝记录的问题：现在会记一条 `gateway/auth_error` 日志（含来源 UA 与脱敏后的收到 key，区分「没带 key」和「带错 key」），并把返回文案改为可操作指引，明确提示应填 Switch Dev「设置 → 接入 apiKey」里的 `rs-` 密钥而非供应商 Key
- 修复配置因某个字段校验失败（如某条 UA 规则的 upstream 无效）被整体重置为默认时顺带**重新生成网关 apiKey**、导致所有已接入客户端静默集体 401 的问题：现在 JSON 能正常解析的前提下，重置会保留原有 apiKey；仅 JSON 真正损坏、无法取回 key 时才生成新 key
- 修复健康探测对 Anthropic 协议的供应商永远用 OpenAI 端点（`/chat/completions` + `Bearer`）探测、导致这类供应商被误判不健康并在降级链中降权的问题：探测现在按供应商协议选择端点与鉴权头（Anthropic 走 `/v1/messages` + `x-api-key`），与实际调用路径一致

### 变更
- 降级链/UA 规则的模型选择器里，自带 Key 接入的第三方供应商模型不再显示「免费」徽章（后端此前统一标 `free:true`，但对用户没有免费语义）；仅内置四上游（JoyCode/DevEco/OpenCode/WorkBuddy）保留该标识，避免与内置免费档混淆

### 安全
- 供应商密钥库新增机器绑定包裹层：DEK 除被随机主密码（存钥匙串）包裹外，另由本机机器码（macOS IOPlatformUUID / Windows MachineGuid / Linux machine-id，经 argon2id 派生）包裹一份，作为抗自更新锁死的始终可用解锁路径。该包裹层仅在「本就允许无密码解锁」时存在（自动加密或勾选记住密码）；用户显式设置主密码且不记住时会被移除，保持「必须输主密码」的安全边界。老用户在下次成功解锁后自动补上这层

## [0.1.2] - 2026-08-20

### 新增
- 凭据页为每个上游（内置四上游 + 免费 API 供应商）增加「启用/停用」开关，全局生效：停用后该上游下所有模型在调用时被直接跳过（降级链自动绕过），卡片显示「⏸ 已停用」徽章；开关状态持久化到 config.json 的 `upstreams` 字段，缺省视为启用
- 日志页「清空」增加二次确认气泡（复用 ConfirmPopover），避免误点丢日志；提示文案说明清空后用量统计仍保留
- 设置页运行配置 JSON 预览升级为完整配置实时预览：展示与当前配置一致的全部字段（含 UA 规则、上游开关、update/logFile/方案等，原来仅 mode/autoChain/manualFallbacks/globalFallback 四项）；apiKey 默认脱敏显示，可点眼睛临时查看明文，复制内容随之带/不带真实密钥

### 变更
- 用量统计与日志表解耦：新增按小时聚合的 `usage_stats` 统计表，写日志时同步累积，启动时把历史日志一次性回填；今日输出速率、按模型/供应商统计、使用趋势、来源统计均改读统计表，清空日志不再影响统计数据（统计口径不变，仅含成功请求，速率为小时桶加权 tok/s）

### 修复
- 修复 JoyCode 全部请求被网关灰度拒绝（AI_GRAY_ACCESS_DENIED「访问受限」）导致 Claude Code 无法对话、但测评界面正常的问题：注入的 clientVersion 取自 state.vscdb 的 `releaseNotes/lastVersion`（应用壳版本 3.0.10），而官方客户端实际注入的是扩展自身 package.json 的 version（getVersion()，3.8.67），灰度按 clientVersion 放行；现改为优先读取扩展 package.json 的真实版本与官方对齐，其次回退 vscdb，最后兜底当前默认版本，彻底清除旧硬编码 3.0.10
- 修复 auto 模式下客户端发具体模型名（如 Claude Code 的 claude-*）时被硬编码路由到 DevEco glm-5.1、完全绕过用户配置的 auto 调用链的问题：auto 模式下所有请求都按 auto 优先级链执行；manual 模式下未识别的模型名不再兜底到 glm-5.1，改用全局兜底（GlobalFallback），同时清理遗留的 AutoModel 常量及相关失效 UI 字段
- 修复 Windows 托盘右键「退出」时卡死、托盘图标残留且无法在任务管理器结束进程的问题：原来在右键菜单的模态消息循环未返回时就同步执行应用销毁，与窗口/托盘/WebView2 的主线程收尾重入死锁；现延迟到菜单关闭、主线程空闲后再退出，并增加 5 秒看门狗，Wails 收尾异常时强制结束进程，杜绝僵尸进程与幽灵托盘图标
- 修复 macOS 自动更新下载替换成功后没有自动重启的问题：新进程作为旧进程子进程未脱离进程组，旧进程退出时会把新进程一起带走；现在用 `setsid` 让新进程独立进程组，且在 `.app` 包内通过 `open` 正常唤起 GUI
- 修复 Windows 自动更新失败 `Access is denied`：安装包原来以管理员权限装到 `C:\Program Files`，而应用运行时是普通用户权限，无法在该目录写入新二进制；改为按用户安装到 `%LOCALAPPDATA%\Programs`（无需 UAC，当前用户可写），自我更新得以正常完成，卸载信息也相应写入 HKCU

## [0.1.1] - 2026-08-19

### 新增
- 添加/编辑供应商时，单个测评和批量测评通过的模型自动加入该供应商，无需再手动逐个点「加入」
- 新增供应商手动拉取模型成功后，默认勾选全部候选模型

### 变更
- 统计页的「使用趋势 / 按供应商统计 / 按模型统计」改为标签页切换，默认显示使用趋势，点击切换，页面不再过长
- 新增供应商时点「✓ 加入通过」批量加入模型后，自动退出编辑态回到供应商列表；单个「加入」仍停留在表单，不打断连续添加多个模型的操作

### 修复
- 修复保存方案时报「无效的 upstream: custom-xxxxxx」：自定义免费 API 供应商的 id 此前被配置校验误拒，导致含自定义供应商的降级链/方案无法保存；校验改为与运行时路由一致
- 修复自动加密模式（未设置主密码）下，钥匙串密码与磁盘密文一旦不匹配就被程序删除、导致本地 API Key 永久无法解锁的问题：解密失败不再删除记住的密码；macOS 改为在系统钥匙串之外额外写一份 0600 文件副本，钥匙串缺失或过期时自动用文件副本恢复并回填
- 修复新增/编辑供应商后，设置页的降级链 upstream 下拉里不出现该供应商：后端模型列表有 10 分钟缓存且未在供应商变更时失效，前端也未监听变化，现供应商增删改后立即刷新，切到设置页即可选到

## [0.1.0] - 2026-08-18

### 新增
- 支持接入免费的大模型 API 供应商（如 Groq、NVIDIA NIM、Gemini 等），评测通过的模型与内置上游（JoyCode/DevEco/OpenCode/WorkBuddy）平级使用，可加入降级链
- 免费 API 供应商支持两种添加方式：从内置目录选择（自动填充地址）或手动自定义（任意 OpenAI 兼容地址）
- 添加供应商时输入 API Key 后逐个拉取候选模型并展示能力信息（上下文窗口、限流/免费方式、工具/视觉/推理徽章），支持跳转官网获取 API Key
- 模型逐个评测通过才加入：真实推理评测验证可用性，通过才进降级链参与路由
- 已通过模型每 5 分钟后台健康监控，不可用的模型在降级链中自动权重降级（排到链尾），不影响内置上游
- 免费 API 目录内置进二进制，运行时自动从 GitHub 拉取最新目录（存本地缓存，一致则不重复写盘；API Key 仅存本地绝不上传）
- 托盘菜单显示应用版本号，新增「去 GitHub 点个 Star」入口
- 托盘菜单支持服务状态展示、方案快速切换、「检查更新」（检查中/已是最新/有新版本带箭头提示）
- 接入 apiKey 支持开关：设置 → 通用中可关闭鉴权，关闭后网关请求无需携带 apiKey，仪表盘首页也不再显示 apiKey（默认开启）

### 变更
- 托盘菜单去掉顶部标题项，服务状态改为纯展示（运行中/已停止），点击无副作用
- 仪表盘与设置页的 apiKey 掩码改为与密钥等长的 `*`（原先固定只显示 3 个 `*`）
- 供应商卡片上的「分享」改为直接进入导出界面并预勾选该供应商，跳过「导出分享 / 导入」选择菜单；顶部「分享/导入」入口保持原菜单不变
- 分享文件（`.sds`）的随机密码改为 6 位字母+数字（去掉易混字符 0/O/l/1/I），并支持在保存前自定义密码；留空保存时才自动生成（原先为 16 位强密码且不可自定义）
- 配置/数据目录从 `~/.config/switch-free`（Windows 为 `%APPDATA%\switch-free`）变更为 `switch-dev`，不再读取或迁移旧目录；移除旧版 `free_apis.json` 文件名自动重命名兼容
- 重写 README 并新增 MIT LICENSE，突出「不生产 Token，只做 Token 的搬运工」定位；README 补充 DevEco 需注册华为开发者并实名认证、其他内置渠道需先在原工具登录并验证可正常调用的前置说明

### 修复
- 修复 Windows 顶部导航栏与标题栏间距过大：50px 隐藏标题栏拖动区改为仅在 macOS 渲染（Windows 使用原生标题栏，无需占位）
- 修复自动加密初始化可能导致供应商凭据永久无法解锁的问题：原先先写磁盘再存钥匙串，若钥匙串写入失败，加密用的随机密码会丢失且无人能解；改为先确认钥匙串写入成功再写磁盘，任一环节失败即回滚内存状态。清除主密码回退自动加密时，若钥匙串写入失败也会把磁盘恢复为旧包裹，避免凭据锁死

## [0.0.6] - 2026-08-11

### 新增
- 运行模式支持多方案：可把当前降级链配置保存为命名方案，在方案间一键切换并立即生效，支持重命名和删除
- 删除降级链条目后可点「撤销」恢复
- 支持openApi的reponse接口

### 变更
- 运行模式的配置项改动后自动保存生效，不再需要单独点「保存配置」
- 移除「保存运行模式」和「重置默认」按钮，界面整体上移；「刷新模型」移到运行模式卡片右上角，方案下拉与保存按钮在其左侧

## [0.0.5] - 2026-08-10

### 新增
- 添加自动更新机制，支持周期性检查更新并区分强制与可选更新


## [0.0.4] - 2025-08-10
### 修复
- 修复 Windows 上托盘右键退出卡死（WindowClosing 钩子在退出时仍 Cancel 导致窗口无法销毁）
- 修复 Windows 上 WorkBuddy 凭据识别不到（文件名实际为 `workbuddy-desktop-ai.info`，非 macOS 的 `workbuddy-desktop.info`）
- 修复 Windows 上 DevEco 凭据无效（Node.js CLI 在 Windows 也用字面 XDG 路径 `~/.config`、`~/.local/share`，之前错误映射到 `%APPDATA%`）
- 修复自动更新后临时文件被提前删除导致 "file not found" 错误（`downloadAsset` 的 `defer Remove` 时机错误）
- 修复 `GetAgents` 漏返回 WorkBuddy 的 valid 状态

### 新增
- 跨平台 agent 安装探测：CLI 工具（DevEco/OpenCode）除凭据文件外，还在 PATH 和 npm 全局目录（`%APPDATA%\npm`）查找可执行文件
- 后台周期探测 agent 安装状态，新安装工具后自动校验凭据并刷新 UI（无需重启）
- 引导弹窗增加「🔄 重新检测」按钮
- 自动更新支持强制更新分级：minor（0.x.0）变化为强制更新，patch（0.0.x）为普通更新
- 自动更新改为启动时及每 6 小时周期检测
- DMG 改为 Universal Binary（同时支持 arm64 和 x86_64 Mac）
- `make deploy` 一键构建裸二进制 + DMG + NSIS 安装包并上传 Release

### 变更
- WorkBuddy 下载链接改为 https://workbuddy.ai
- OpenCode 安装命令改为 `npm i -g opencode-ai`（跨平台，替代仅 macOS 的 brew）
- 移除仅含 macOS 路径的硬编码 ProbePaths，改用 paths 包跨平台候选路径（单一真相）

## [0.0.3] - 2025-08-08
### 验证
- 四上游凭据全部有效（JoyCode / DevEco / OpenCode / WorkBuddy）
- `/v1/messages`、`/v1/chat/completions` 流式/非流式正常
- 首页速率统计正常

## [0.0.2]
### 修复
- JoyCode sqlite 读取从外部 sqlite3 CLI 改为 `modernc.org/sqlite` 纯 Go 驱动（跨平台，无需额外依赖）

## [0.0.1]
### 新增
- 初始版本：JoyCode / DevEco / OpenCode / WorkBuddy 四上游支持
- 跨平台桌面应用（macOS / Windows），系统托盘常驻
- 自动降级链（auto 模式）+ 手动模型选择
- 自动更新（GitHub Releases）
- 模型测评、请求日志、用量统计
